
checkout:
  post:
    - nvm install "$(< .nvmrc)"
dependencies:
  pre:
    - nvm install "$(< .nvmrc)"
  services:
    - postgresql
machine:
  environment:
    CIRCLE_ENV: test
    NODE_ENV: test
    DATABASE_URL: postgresql://ubuntu:@127.0.0.1:5432/circle_test
    APP_NAME: circle_test
